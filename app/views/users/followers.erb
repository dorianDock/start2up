<% # encoding: utf-8 %>
<h1>Vos Followers</h1>
<% unless @users.any? %>
    <h2>Section vide pour l'instant: encore un peu de patience :)</h2>
<% else %>
    <h2>N'hesitez pas à suivre des entrepreneurs pour accéder à leur actualité plus facilement !</h2>
<% end %>


<%= will_paginate %>

<div class="ui three stackable special cards">
  <% @users.each do |user| %>
      <%= render 'simpleUserCard', :user => user %>
  <% end %>
</div>

<%= will_paginate %>