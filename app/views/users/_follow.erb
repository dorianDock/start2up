<% # encoding: utf-8 %>

<%= form_for current_user.askedFriendships.build(:answererId => user.id) do |f| %>
    <%= f.hidden_field :answererId %>
    <%= f.submit "Suivre", class: "ui inverted button" %>
<% end %>