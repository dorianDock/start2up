<h1>Liste des requetes</h1>


<div class="">
  <h1>Envoyees</h1>
  <table class="ui celled table">
    <thead>
    <tr>
      <th>A</th>
      <th>Quand ?</th>
      <th>Vous avez accepte ?</th>
      <th>Type</th>
    </tr>
    </thead>
    <tbody>
    <% @sent_requests.each do |request| %>
        <tr>
          <td><%= request.answerer.name %></td>
          <td><%= request.created_at.strftime("Envoyee le %d/%m/%Y a %H:%M") %></td>
          <td><%=
              request.isAccepted != nil ? request.isAccepted : 'En attente de reponse'
          %></td>
          <td><%= request.user_link_type.title %></td>
        </tr>
    <% end %>
    </tbody>
  </table>

  <h1>Recues</h1>
  <table class="ui celled table">
    <thead>
    <tr class="blue column">
      <th>De</th>
      <th>Quand ?</th>
      <th>Vous avez accepte ?</th>
      <th>Type</th>
    </tr>
    </thead>
    <tbody class="receivedRequestsTBody">
    <% @received_requests.each do |user_link| %>

        <%= render partial: "requestLine", locals: { user_link: user_link } %>

    <% end %>
    </tbody>
  </table>
</div>

